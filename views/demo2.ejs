<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <title>Document</title>
    <style>
        .card-container{
            /* display: flex; */
            /* flex-wrap: wrap; */
            justify-content: center;
            /* margin-top: 10px; */
            gap: 15px;  

        }
        .card{
            border: 1px solid black ;
            border-radius: 15px;
            padding:20px ;
            /* width: 250px; */
            background-color: rgb(255, 117, 198);

            
        }


    </style>

</head>
<body>
<h1>Product from</h1>
<form action="/" method="post" class="container">
    <input type="text" name="productname" placeholder=" Productname "class="saran" >
    <input type="text" name="productdetails" placeholder="productdetails"  class="saran">
    <input type="text"  name= "productprice"  placeholder="productprice" class="saran">
    <input type="text"  name= "productquality"placeholder="productquality" class="saran">
    <input type="text" name="productowner" placeholder="productowner" class="saran">
    
    <input type="submit" value="submit"class="btn btn-primary">

</form>

   <!-- <% if (!users || users.length==0){%>
    <h1 class="text-center">
        No Data Found
    </h1
   <% }else {%>
     <div class="row"> 
        <div class="col">
        <% users.forEach(user=>{ %>
            <div class="card" style="width: 15rem;">
                <div class="card-body">
                  <h5 class="card-title">
                    <%=user.productname%>
                    <br>
                    <%=user.productdetails%>
                    <br>
                    <%=user.productprice%>
                    <br>
                    <%=user.productquality%>
                    <br>
                    <%=user.productowner%>
                    
                  </h5>
                  <h6 class="card-subtitle mb-2 text-body-secondary">Card subtitle</h6>
                  <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
                  <a href="#" class="card-link">Card link</a>
                  <a href="#" class="card-link">Another link</a>
                </div>
              </div>
    </div>

<%}) ;%>
     </div>
     <%}%>
     </div> -->


     <!-- <% if (!users || users.length == 0) { %>
        <h1 class="text-center">No Data Found</h1>
      <% } else { %>
        <div class="container mt-4">
          <div class="row justify-content-center gap-3">
            <% users.forEach(user => { %>
              <div class="card" style="width: 18rem;">
                <div class="card-body">
                  <h5 class="card-title"><%= user.productname %></h5>
                  <p class="card-text">
                    <strong>Details:</strong> <%= user.productdetails %><br>
                    <strong>Price:</strong> <%= user.productprice %><br>
                    <strong>Quality:</strong> <%= user.productquality %><br>
                    <strong>Owner:</strong> <%= user.productowner %>
                  </p>
                </div>
              </div>
            <% }); %>
          </div>
        </div>
      <% } %> -->


      <% users.forEach((user, index) => { %>
        <div class="card m-2" style="width: 18rem;">
          <div class="card-body">
            <h5 class="card-title"><%= user.productname %></h5>
            <p class="card-text">
              <strong>Details:</strong> <%= user.productdetails %><br>
              <strong>Price:</strong> <%= user.productprice %><br>
              <strong>Quality:</strong> <%= user.productquality %><br>
              <strong>Owner:</strong> <%= user.productowner %>
            </p>
      
            
            <form action="/delete" method="POST">
              <input type="hidden" name="index" value="<%= index %>">
              <button type="submit" class="btn btn-danger btn-sm">Delete</button>
            </form>
          </div>
        </div>
      <% }); %>
      
      
    
</body>
</html>
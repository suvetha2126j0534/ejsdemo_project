<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            text-align: center;
            font-family: Arial, sans-serif;
            background: #f7f7f7;
        }
        .box {
            margin: 20px auto;
            padding: 20px;
            border: 1px solid #ccc;
            width: 350px;
            border-radius: 8px;
            background: white;
        }
        .balance {
            font-weight: bold;
            margin: 10px 0;
        }
        .expense-item {
            display: flex;
            justify-content: space-between;
            padding: 5px 10px;
            border: 1px solid #eee;
            margin: 5px 0;
            border-left: 5px solid green;
        }
    </style>
</head>
<body>

<h1 style="color: purple;">Expense Tracker</h1>

<div class="box">
    <div class="d-flex justify-content-around mb-3">
        <div>
            <strong>INCOME</strong><br>
            <span style="color: green;">$<%= income.toFixed(2) %></span>
        </div>
        <div>
            <strong>EXPENSE</strong><br>
            <span style="color: red;">$<%= totalExpense.toFixed(2) %></span>
        </div>
    </div>

    <form action="/add-income" method="POST">
        <input type="number" name="incomeAmount" placeholder="Enter Your Income" class="form-control mb-2" required>
        <button type="submit" class="btn btn-success w-100 mb-3">Add Income</button>
    </form>

    <div class="balance">
        TOTAL BALANCE: $<%= balance.toFixed(2) %>
    </div>

    <form action="/add-expense" method="POST">
        <input type="text" name="item" placeholder="Enter Item" class="form-control mb-2" required>
        <input type="number" name="amount" placeholder="Amount" class="form-control mb-2" required>
        <button type="submit" class="btn btn-primary w-100">Add Expenses</button>
    </form>

    <% expenses.forEach(exp => { %>
        <div class="expense-item">
            <span><%= exp.item %></span>
            <span>$<%= exp.amount.toFixed(2) %></span>
        </div>
    <% }) %>
</div>

</body>
</html> -->



<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            text-align: center;
            font-family: Arial, sans-serif;
        }
        .box {
            margin: 20px auto;
            padding: 20px;
            border: 1px solid #ccc;
            width: 350px;
            border-radius: 8px;
        }
        .balance {
            font-weight: bold;
            margin: 10px 0;
        }
        .expense-item {
            display: flex;
            justify-content: space-between;
            padding: 5px 10px;
            border: 1px solid #eee;
            margin: 5px 0;
            border-left: 5px solid green;
        }
    </style>
</head>
<body>

<h1 style="color: purple;">Expense Tracker</h1>

<div class="box">
    <div class="d-flex justify-content-around mb-3">
        <div>
            <strong>INCOME</strong><br>
            <span style="color: green;">$<%= income.toFixed(2) %></span>
        </div>
        <div>
            <strong>EXPENSE</strong><br>
            <span style="color: red;">$<%= totalExpense.toFixed(2) %></span>
        </div>
    </div>

    <form action="/add-income" method="POST">
        <input type="number" name="incomeAmount" placeholder="Enter Your Income" class="form-control mb-2" required>
        <button type="submit" class="btn btn-success w-100 mb-3">Add Income</button>
    </form>

    <div class="balance">
        TOTAL BALANCE: $<%= balance.toFixed(2) %>
    </div>

    <form action="/add-expense" method="POST">
        <input type="text" name="item" placeholder="Enter Item" class="form-control mb-2" required>
        <input type="number" name="amount" placeholder="Amount" class="form-control mb-2" required>
        <button type="submit" class="btn btn-primary w-100">Add Expenses</button>
    </form>

    <% expenses.forEach(exp => { %>
        <div class="expense-item">
            <span><%= exp.item %></span>
            <span>$<%= exp.amount.toFixed(2) %></span>
        </div>
    <% }) %>
</div>

</body>
</html>